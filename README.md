# Basic Swift iOS App

A minimal SwiftUI iOS app for exercising `depef` workflows against a local app target.

Build from this directory with a simulator destination, for example:

```sh
xcodebuild -project basic-swift-ios-app.xcodeproj -scheme basic-swift-ios-app -configuration Debug -destination 'generic/platform=iOS Simulator' build
```
